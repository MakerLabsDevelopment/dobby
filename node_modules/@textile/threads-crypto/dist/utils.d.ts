export declare function multihash(digest: Uint8Array, code: number, length?: number): Uint8Array;
/**
 * Basic multihash implementation that only supports sha256.
 * @param bytes The bytes to wrap with multihash.
 */
export declare function sha256Multihash(bytes: Uint8Array): Uint8Array;
export declare function ensureKey(key: Uint8Array, length: number): Uint8Array;
